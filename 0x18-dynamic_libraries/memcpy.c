#include "main.h"
#include <stdlib.h>

/**
  * memcpy - check if the num is absolute
  *
  * @num: the number to be checked
  * Return: return 1 if true and 0 if false
  */

char *_memcpy(char *dest, char *src, unsigned int n)
{
        return (dest);
}

