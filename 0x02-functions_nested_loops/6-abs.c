#include "main.h"
#include <stdlib.h>

/**
  * _abs - check if the num is absolute
  *
  * @num: the number to be checked
  * Return: return 1 if true and 0 if false
  */

int _abs(int num)
{
	return (abs(num));
}
